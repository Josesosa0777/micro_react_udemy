# On folders post and comments:

```
npm init -y
npm install express cors axios nodemon
```

on client folder:
```
npm install axios
```

Open a terminal for each post, comments and client directory and execute:
```
npm start
```